# Fuddly - Маркетплейс продуктов питания

Современный B2B маркетплейс для оптовой торговли продуктами питания. Платформа объединяет производителей и оптовых покупателей.

## 🚀 Технологии

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Redux Toolkit** - управление состоянием
- **React Router** - маршрутизация
- **Vite** - сборщик
- **SCSS** - стилизация

### Backend
- **Node.js + Express** - REST API
- **TypeScript** - типизация
- **Supabase** - база данных (PostgreSQL) и аутентификация
- **Zod** - валидация данных

### Архитектура
- **Feature-Sliced Design** - архитектура фронтенда
- **Row Level Security (RLS)** - безопасность на уровне БД

---

## 📦 Установка и запуск

### Требования
- Node.js >= 18
- npm >= 9
- Аккаунт Supabase (бесплатный)

### 1. Клонирование репозитория
```bash
git clone <repo-url>
cd fuddly_supabase
```

### 2. Установка зависимостей
```bash
make install
```

Или вручную:
```bash
cd backend && npm install
cd ../frontend && npm install
```

### 3. Настройка Supabase

#### 3.1. Создание проекта
1. Перейдите на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Скопируйте URL и API ключи из `Settings > API`

#### 3.2. Настройка базы данных
1. Откройте `SQL Editor` в Supabase Dashboard
2. Выполните SQL скрипт из файла `backend/supabase/schema.sql`
3. Это создаст все таблицы и RLS политики

### 4. Настройка переменных окружения

Скопируйте пример конфигурации:
```bash
cp backend/.env.example backend/.env
```

Откройте `backend/.env` и заполните:
```bash
# Supabase (из Settings > API вашего проекта)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-role-key

# Server
PORT=3003
NODE_ENV=development
```

### 5. Запуск проекта
```bash
make start
```

Или вручную:
```bash
# Терминал 1 - Backend
cd backend && npm run dev

# Терминал 2 - Frontend
cd frontend && npm run dev
```

Приложение будет доступно:
- Frontend: http://localhost:5173
- Backend API: http://localhost:3003
- Health check: http://localhost:3003/health

---

## 🏗️ Структура проекта

```
fuddly_supabase/
├── backend/                   # Backend приложение
│   ├── src/
│   │   ├── controllers/      # Контроллеры API
│   │   │   ├── authController.ts
│   │   │   ├── productController.ts
│   │   │   ├── leadController.ts
│   │   │   └── messageController.ts
│   │   ├── middleware/       # Middleware (аутентификация)
│   │   ├── routes/           # Маршруты API
│   │   ├── supabase.ts       # Supabase клиент
│   │   └── index.ts          # Точка входа
│   ├── supabase/
│   │   └── schema.sql        # SQL схема БД
│   ├── .env.example          # Пример конфигурации
│   ├── package.json
│   └── tsconfig.json
│
├── frontend/                  # Frontend приложение
│   ├── src/
│   │   ├── app/              # Конфигурация приложения
│   │   │   ├── store/        # Redux store
│   │   │   │   ├── slices/   # Redux slices
│   │   │   │   ├── hooks.ts  # Типизированные хуки
│   │   │   │   └── index.ts  # Конфигурация store
│   │   │   ├── styles/       # Глобальные стили
│   │   │   └── App.tsx       # Главный компонент
│   │   ├── pages/            # Страницы
│   │   │   ├── auth/         # Авторизация
│   │   │   ├── catalog/      # Каталог товаров
│   │   │   ├── home/         # Главная
│   │   │   ├── product-detail/
│   │   │   └── profile/      # Профиль пользователя
│   │   ├── widgets/          # Виджеты (Header, Footer)
│   │   ├── shared/           # Общий код
│   │   │   ├── api/          # API клиенты
│   │   │   ├── types/        # TypeScript типы
│   │   │   └── styles/       # Переменные стилей
│   │   └── main.tsx          # Точка входа
│   ├── package.json
│   ├── tsconfig.json
│   └── vite.config.ts
│
├── Makefile                   # Команды для разработки
├── .gitignore
└── README.md
```

---

## 📚 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получить текущего пользователя
- `POST /api/auth/make-admin` - Стать админом (только для первого пользователя)

### Товары
- `GET /api/products` - Список товаров (с фильтрами)
- `GET /api/products/:id` - Один товар
- `GET /api/products/my` - Мои товары
- `POST /api/products` - Создать товар
- `PATCH /api/products/:id` - Обновить товар
- `DELETE /api/products/:id` - Удалить товар

### Лиды
- `GET /api/leads` - Список лидов
- `POST /api/leads` - Создать лид

### Сообщения
- `GET /api/messages` - Список сообщений
- `POST /api/messages` - Отправить сообщение
- `PATCH /api/messages/:id/read` - Пометить как прочитанное

---

## 🛠️ Доступные команды

```bash
make install        # Установить все зависимости
make start          # Запустить backend и frontend
make stop           # Остановить все процессы
make clean          # Удалить node_modules и dist
make build          # Собрать production версию
```

---

## 🗄️ База данных

### Таблицы
- **users** - Пользователи
- **products** - Товары
- **leads** - Лиды (заявки на покупку)
- **messages** - Сообщения между пользователями
- **reports** - Жалобы

### Статусы
- **Пользователь**: ACTIVE, BLOCKED, PENDING_VERIFICATION
- **Товар**: PENDING (на модерации), APPROVED (одобрен), REJECTED (отклонён)
- **Лид**: PENDING, CONTACTED, CLOSED

### Row Level Security (RLS)
Все таблицы защищены RLS политиками:
- Пользователи видят только активных пользователей
- Товары: все видят одобренные, продавец видит свои
- Лиды: покупатель и продавец видят только свои
- Сообщения: только отправитель и получатель

---

## 🔐 Безопасность

### Backend
- JWT токены от Supabase Auth
- Middleware проверка токенов
- Валидация данных через Zod

### Database
- Row Level Security (RLS) на всех таблицах
- Проверка `auth.uid()` в политиках
- Индексы для производительности

### Frontend
- Токен хранится в localStorage
- Автоматическая авторизация при наличии токена
- Защищённые маршруты

---

## 🐛 Troubleshooting

### Backend не запускается
1. Проверьте `backend/.env` - все переменные заполнены?
2. Убедитесь что порт 3003 свободен
3. Проверьте Supabase ключи

### Frontend не подключается к API
1. Проверьте `frontend/vite.config.ts` - proxy настроен на порт 3003
2. Убедитесь что backend запущен
3. Откройте Network в DevTools браузера

### Ошибки CORS
Backend уже настроен с `cors()`. Если проблема остаётся:
- Проверьте что используете правильный URL (через proxy `/api`)
- Убедитесь что backend запущен

---

## 📝 Лицензия

ISC

---

## 👨‍💻 Разработка

### Добавление нового функционала
1. Backend: создайте контроллер в `backend/src/controllers/`
2. Добавьте маршрут в `backend/src/routes/`
3. Подключите маршрут в `backend/src/index.ts`
4. Frontend: создайте slice в `frontend/src/app/store/slices/`
5. Создайте API клиент в `frontend/src/shared/api/`
6. Добавьте страницу в `frontend/src/pages/`

### Git workflow
```bash
git checkout -b feature/your-feature
# ... делайте изменения
git add .
git commit -m "feat: add your feature"
git push origin feature/your-feature
```

---

## 🎯 Roadmap
- [ ] Загрузка изображений товаров
- [ ] Фильтры и сортировка в каталоге
- [ ] Чат между пользователями в реальном времени
- [ ] Админ-панель для модерации
- [ ] Email уведомления
- [ ] Статистика и аналитика
